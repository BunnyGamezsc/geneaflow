import type { FamilyNode } from "./types.ts";

export const NODE_WIDTH = 160;
export const NODE_HEIGHT = 90;
export const LEVEL_HEIGHT = 200;
export const SIBLING_GAP = 200;

export const INITIAL_NODE: FamilyNode = {
  id: "1",
  name: "Me",
  gender: "neutral",
  x: 0,
  y: 0,
};
